server:
  port: 8094
 
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8095/eureka/

management:
  endpoints:
    web:
      exposure:
        include: refresh

leekyoungil:
  profile: i'm local
  said:
    first:  local
    second: local
